#include <stdio.h>
#include <stdlib.h>

int soma(int num1, int num2, int num3)
{
    
}

int main()
{
    int num1, num2, num3;
    scanf("%d %d %d", &num1, &num2, &num3);

    int res = soma(num1, num2, num3);
    printf("%d", res);
}